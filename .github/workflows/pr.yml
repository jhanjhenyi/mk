name: Run tests

on:
  push:
  pull_request:
  schedule:
    - cron: '*/2 * * * *'  # every day at midnight

jobs:
  deploy:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2

      - uses: actions/checkout@v2

      # Make changes to pull request here

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3
