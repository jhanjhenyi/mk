name: Release

on:
  pull_request:
    branches:
      - master
    types: [closed]


jobs:
  build:
    name: Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
